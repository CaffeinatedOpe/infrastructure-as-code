kind: Kustomization
namespace: flux-system
resources:
  - deployment.yaml
configMapGenerator:
  - name: jenkins-config
    files:
      - values.yaml=values.yaml
configurations:
  - kustomizeconfig.yaml