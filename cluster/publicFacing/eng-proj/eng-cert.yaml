apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: eng-cert
  namespace: eng
spec:
  # Secret names are always required.
  secretName: eng-cert
  issuerRef:
    name: cf-cluster-issuer
    kind: ClusterIssuer
  dnsNames:
    - 6hr.caffeinatedope.net
